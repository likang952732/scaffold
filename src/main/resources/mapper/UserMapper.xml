<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wwinfo.mapper.UserMapper">

    <select id="getByUserName" parameterType="string" resultType="com.wwinfo.model.User">
        select
            userName,
            userType,
            orgID,
            nickname,
            password,
            neverLogin,
            timeLastLogin,
            lastLoginIP,
            timeAdd,
            timeModify,
            errorTimes,
            isLocked,
            timeLocked,
            email,
            realName,
            phone,
            address,
            headUrl,
            sex,
            remark,
            isStop,
            timeFirstLogin
        from user where userName = #{userName}
    </select>

</mapper>
