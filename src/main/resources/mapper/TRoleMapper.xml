<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wwinfo.mapper.TRoleMapper">

    <select id="getUmsRole" resultType="com.wwinfo.model.UserRole">
        SELECT role_id from t_admin_role where user_id=#{adminId,jdbcType=VARBINARY}
    </select>

    <select id="getRoleList" resultType="com.wwinfo.model.TRole">
        select user_id from t_admin_role where role_id=#{roleId,jdbcType=VARBINARY}
    </select>

</mapper>